#pragma once


namespace gca{
	
	class GCA_bivector{
	private:
		
	public:
		// Constructeur
		GCA_bivector();
		// Destructeur
		~GCA_bivector();
		
	/*	operator=();
		TriVector operator^(const BiVector& other) const;
		operator~();
		operator<<();*/
	};
}
